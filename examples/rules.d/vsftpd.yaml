# vsftpd brute force.
rules:
  - name: vsftpd
    source: vsftpd-log
    regex: 'vsftpd: \(pam_unix\) authentication failure;.*rhost=(?P<ip>\S+)'
    max_retries: 5
    findtime: 10m
    bantime: 12h
