# Dovecot IMAP / POP3 brute force.
rules:
  - name: dovecot
    source: mail-log
    regex: 'dovecot: (?:imap|pop3)-login: (?:Aborted login|Disconnected)(?: \([^)]*\))?: .* rip=(?P<ip>[^,]+)'
    max_retries: 5
    findtime: 10m
    bantime: 12h
